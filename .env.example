# Google OAuth Configuration
# Instrucciones para configurar Google Calendar OAuth:
#
# 1. Ve a Google Cloud Console: https://console.cloud.google.com/
# 2. Crea un nuevo proyecto o selecciona uno existente
# 3. Habilita la API de Google Calendar:
#    - Ve a "APIs & Services" > "Enable APIs and Services"
#    - Busca "Google Calendar API" y actívala
# 4. Configura la pantalla de consentimiento OAuth:
#    - Ve a "APIs & Services" > "OAuth consent screen"
#    - Selecciona "External" y completa la información básica
#    - En "Scopes", agrega estos scopes:
#      * https://www.googleapis.com/auth/calendar
#      * https://www.googleapis.com/auth/calendar.events
#      * https://www.googleapis.com/auth/userinfo.email
# 5. Crea credenciales OAuth 2.0:
#    - Ve a "APIs & Services" > "Credentials"
#    - Haz clic en "Create Credentials" > "OAuth client ID"
#    - Selecciona "Web application"
#    - En "Authorized redirect URIs", agrega:
#      * http://localhost:3000/api/auth/callback/google (para desarrollo)
#      * https://tu-dominio.com/api/auth/callback/google (para producción)
# 6. Copia el Client ID y Client Secret a las variables a continuación

# Client ID de Google OAuth (visible en el navegador)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com

# Client ID y Secret para el servidor (solo backend, NUNCA exponer)
GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu-client-secret

# URL de la aplicación (para OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NOTAS IMPORTANTES:
# - El GOOGLE_CLIENT_ID y NEXT_PUBLIC_GOOGLE_CLIENT_ID deben tener el mismo valor
# - NUNCA expongas GOOGLE_CLIENT_SECRET en el frontend
# - La URL de redirect DEBE estar exactamente registrada en Google Cloud Console
# - Para producción, actualiza NEXT_PUBLIC_APP_URL con tu dominio real
# - Asegúrate de agregar AMBAS URLs de redirect (localhost Y producción) en Google Console
